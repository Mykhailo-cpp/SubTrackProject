<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="004" author="subtrack">
        <comment>
            Add renewal_reminder_enabled to subscriptions.
            Defaults to false — existing subscriptions are opted out by default,
            requiring explicit user action to enable reminders (GDPR consent).
        </comment>

        <addColumn tableName="subscriptions">
            <column name="renewal_reminder_enabled"
                    type="BOOLEAN"
                    defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </addColumn>
    </changeSet>

</databaseChangeLog>
